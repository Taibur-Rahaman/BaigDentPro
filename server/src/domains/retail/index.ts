/**
 * Retail — ecommerce catalog stock (`ShopProduct`), tenant products, orders.
 */
export { default as shopRoutes } from '../../routes/shop.js';
export { default as productRoutes } from '../../routes/products.js';
export { default as orderRoutes } from '../../routes/orders.js';
