/**
 * Clinical domain — EMR surfaces only (no finance routers).
 */
export { default as patientRoutes } from '../../routes/patients.js';
export { default as prescriptionRoutes } from '../../routes/prescriptions.js';
export { default as labRoutes } from '../../routes/lab.js';
