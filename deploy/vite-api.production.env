# Public production API base (no secrets). Used when CI/Hostinger builds without .env.production.
# Override locally via .env.production or environment variables.
VITE_API_BASE_URL=https://api.baigdentpro.com/api
