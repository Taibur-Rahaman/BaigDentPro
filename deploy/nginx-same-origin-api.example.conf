# Example Nginx fragment: static BaigDentPro SPA + proxy /api to Node (same domain).
# Adjust paths, server_name, TLS cert paths, and upstream port (default app PORT=3001).
#
# Why: If you only upload dist/ to static hosting, /api/health returns HTML (SPA).
# Same-origin /api avoids CORS issues and lets you omit VITE_API_URL in the build.

# server {
#   listen 443 ssl http2;
#   server_name baigdentpro.com www.baigdentpro.com;
#
#   root /var/www/baigdentpro/dist;
#   index index.html;
#
#   location /api/ {
#     proxy_pass http://127.0.0.1:3001/api/;
#     proxy_http_version 1.1;
#     proxy_set_header Host $host;
#     proxy_set_header X-Real-IP $remote_addr;
#     proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
#     proxy_set_header X-Forwarded-Proto $scheme;
#   }
#
#   location / {
#     try_files $uri $uri/ /index.html;
#   }
# }
