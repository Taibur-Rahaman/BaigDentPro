/**
 * Workspace prescription directory (sorted VM list + aggregates).
 * List fetching + mapping remains in {@link usePracticeDashboardBundle}.
 */
export { usePrescriptionsWorkspaceView as usePrescriptionsView } from './usePrescriptionsWorkspaceView';
