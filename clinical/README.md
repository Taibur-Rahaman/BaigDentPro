# Clinical domain (documentation anchor)

Canonical Express grouping: `server/src/domains/clinical/index.ts` → `/api/patients`, `/api/prescriptions`, `/api/lab`.

Clinical inventory (consumables) is **not** implemented here yet — do not use `ShopProduct` for it.
