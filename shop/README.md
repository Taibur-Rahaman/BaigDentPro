# Retail / shop domain (documentation anchor)

Canonical grouping: `server/src/domains/retail/index.ts` → `/api/shop`, `/api/products`, `/api/orders`.

Catalog stock (`ShopProduct`) is **retail ecommerce**, not clinical supply inventory.
