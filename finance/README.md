# Finance domain (documentation anchor)

**Patient AR:** `server/src/domains/finance/patientAccountsReceivable.ts` → `/api/invoices`.

**Platform / SaaS settlement:** `server/src/domains/finance/platformSaas.ts` → `/api/billing`, `/api/subscription`, `/api/payment`, `/api/admin/subscription-payments`.

Patient statement UI ≠ general ledger — see `docs/architecture/DOMAIN_AUDIT.md`.
